apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  labels:
    app.kubernetes.io/instance: devworkspace-operator-subscription
    app.kubernetes.io/managed-by: devworkspace-operator
    app.kubernetes.io/name: devworkspace-operator-subscription
  name: devworkspace-operator-subscription
  namespace: {{ .Values.operatorNamespace}}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.syncwave | default "-2" | quote }}
spec:
    channel: {{ .Values.channel }}
    installPlanApproval: {{ .Values.installPlan }}
    name: devworkspace-operator
    source: redhat-operators
    sourceNamespace: openshift-marketplace
